<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <h1>
	<%= image_tag 'callgov.png', :alt => 'callgov.us', :class => 'hidden-sm hidden-xs' %>
	<a href="/" class="navbar-brand">
	  <%= fa_icon 'phone' %>
          CallGov.us
	</a>
      </h1>
    </div>

    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li>
	  <a href="<%= new_script_path %>" class='btn btn-primary navbar-btn'>
            <span class="glyphicon glyphicon-plus"></span>
            New Script
	  </a>
        </li>
        <% if user_signed_in? %>
          <li>
            <span class='logged-in-user'>Signed in as <%= current_user.email %>     <img src="<%= current_user.picture %>" width="30" height="30" />
	      You've made
              <div class='badge'>
                <%= current_user.calls.size %>
              </div>
              calls so far&mdash;keep it up!
            </span>
          </li>
          <li><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
        <% else %>
          <li><%= render :partial => 'devise/shared/links' %></li>
        <% end %>
      </ul>

      	<h1 class='title'>Call Your Senators and Representatives in Congress</h1>

    </div>
  </div>
</nav>
<p class='clearing'>&nbsp;</p>

