<div class='row'>
  <div class='col-md-6 col-xs-12'>
    <h2>Ready to Call</h2>
  </div>
  <div class='col-md-6 col-xs-12'>
  </div>
</div>
<div class="well">
  <div class='row'>
    <div class='col-md-3 col-xs-12'>
      <%= representative.display_image %>
    </div>
    <div class='col-md-9 col-xs-12'>
      
      <div class='display-name
	  <%= 'democrat' if representative.party == 'D' %>
	  <%= 'republican' if representative.party == 'R' %>
	  '>
	<%= representative.display_name %>
      </div>
    </div>
    <div class='col-xs-12 mvm'>
      <span class="phone-number btn btn-primary" id="rep-phone"><%= representative.phone %></span>

      <div class='muted-text'>
	<small>Click to call if you're on a phone, or to use a program like Skype; otherwise, just pick up your phone and dial the number. Then click "Record my call!" below to record your call.</small>
      </div>

      
          <form id="record_call">
      <input type="hidden" name="rep_id" value="<%= representative.bioguide_id %>" />
      <input type="hidden" name="address" value="<%= params[:address] %>" />
      <input type="hidden" name="i_called" value="yes" />
      <p class='mvm'>
	<button type="submit" class="complete-call btn btn-primary btn-lg"><span class='fa fa-check-square-o' aria-hidden="true"></span> Record my call!</button>
      </p>
    </form>
    </div>
  </div>
</div>
